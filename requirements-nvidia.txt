# requirements-nvidia.txt (For NVIDIA GPU with CUDA 12.8 / Blackwell support)
# PyTorch is installed separately in the Dockerfile for better layer caching.
# Do NOT pin torch/torchvision/torchaudio here.

# NOTE: chatterbox-tts is installed with --no-deps in the Dockerfile
# to prevent it from downgrading PyTorch from 2.7+cu128 to 2.5.1

# Core Web Framework
fastapi
uvicorn[standard]

# Other ML & Audio Libraries
numpy>=1.24.0
soundfile  # Requires libsndfile system library
librosa
safetensors
descript-audio-codec

# Configuration & Utilities
PyYAML
python-multipart
requests
Jinja2
watchdog
aiofiles
unidecode
inflect
tqdm
hf_transfer                     # Speed up HuggingFace downloads

# Audio Post-processing
pydub
audiotsm
praat-parselmouth
